#ifndef _COMMON_H
#define _COMMON_H

#define ARRAY_LENGTH(arr) (sizeof(arr) / sizeof((arr)[0]))

#endif

#include "error.h"
#include "vector.h"
#include "character.h"
#include "input.h"
#include "tokenizer.h"
#include "parser.h"
